[package]
name = "proxy-contract-tests"
version = "0.1.0"
edition = "2024"
publish = false

[lints]
workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
